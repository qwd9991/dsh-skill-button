## Summary

<!-- What does this PR do? One paragraph. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run build` passes
- [ ] `npm run check:public` passes (no host-only paths/credentials)
- [ ] README updated if user-facing behavior changed
